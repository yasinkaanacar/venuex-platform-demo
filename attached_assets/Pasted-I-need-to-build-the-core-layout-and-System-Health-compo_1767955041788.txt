I need to build the core layout and "System Health" components for VenueX.
This includes the Top Navigation (Pipeline) and a Permanent Right Sidebar (Activity Log).

**Visual Style Constraints:**
- Background: Dark Mode (bg-[#111827]).
- Panel Backgrounds: Slightly lighter (bg-[#1F2937]).
- Borders: Subtle dark gray borders (border-gray-700).
- Text: Inter font, readable sizes.

**Component 1: The `PipelineHeader` (Top Full-Width Bar)**
- **Layout:** Flex row, items centered vertically.
- **Content (Left to Right):**
    1.  **Source Node:** "Client ERP" (Green Dot Indicator). Hover text: "Ping: 24ms".
    2.  **Connector 1:** An animated horizontal line (use CSS keyframes for a 'flowing data' effect) connecting ERP to VenueX.
    3.  **Processor Node:** "VenueX Engine" (Blue Lightning Icon). Subtext: "Processing...".
    4.  **Connector 2:** Animated line flowing to destinations.
    5.  **Destination Nodes:** "Google Merchant" & "Meta Commerce" (Logos + Status Dots).
- **Design Note:** This header should look like a monitoring tool.

**Component 2: The `LiveActivitySidebar` (Fixed Right Panel)**
- **Position:** Placed on the right side of the screen, occupying 300px width, full height (minus header).
- **State:** ALWAYS OPEN (visible). No close buttons.
- **Header:** Title "Live Stream" with a small pulsating green dot.
- **Content:** A scrolling list of log entries.
    - Style: Monospaced font (text-xs or text-sm).
    - Rows: Time stamp + Message + Status Code.
    - Example: `14:32:01 | SKU-9921 updated on Meta [200 OK]`
    - Color coding: Use subtle green for success, yellow for warnings in the text.

**Layout Implementation:**
- Create a `DashboardLayout` component that arranges:
    - `PipelineHeader` at the top (fixed height, e.g., 64px).
    - `LiveActivitySidebar` on the right (fixed width, 300px).
    - A `MainContentArea` in the remaining space (Left empty for now).

**Technical:**
- Use Tailwind CSS.
- Ensure the sidebar has `overflow-y-auto` so logs can scroll.
- Add a "Simulate Log" effect if possible (e.g., a `useEffect` that adds a dummy log every 3 seconds) to demonstrate the "Live" feeling.