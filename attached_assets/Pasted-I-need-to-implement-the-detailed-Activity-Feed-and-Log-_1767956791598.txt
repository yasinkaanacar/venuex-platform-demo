I need to implement the detailed "Activity Feed" and "Log Detail Drawer" for the VenueX dashboard.

**Design Philosophy:**
- **Sidebar:** A vertical timeline connecting events. NOT a chaotic log stream.
- **Drawer:** A slide-over panel for deep dives. Focus on "Grouping Errors" rather than listing them.

**Component 1: ActivityFeedSidebar (The Timeline)**
- **Structure:**
    - Fixed right sidebar (w-80), dark bg (gray-900).
    - Content is a list of "Event Cards" connected by a vertical line on the left.
- **Card States (Mock Data needed):**
    1.  **Processing:** Blue pulse icon. Text: "Ingesting from ERP... (45%)".
    2.  **Success:** Green check icon. Title: "Meta Sync Complete". Subtext: "5k items updated".
    3.  **Warning (The focus):** Yellow alert icon. Title: "Google Sync Finished". Subtext: "120 Issues found". **Action:** A "Review" button that triggers the Drawer.
    4.  **Failure:** Red X icon. Title: "Connection Failed". Subtext: "Invalid Token". **Action:** "Retry".

**Component 2: LogDetailDrawer (The Slide-Over)**
- **Behavior:** Slides in from right, covers 40% of screen, backdrop blur.
- **Header Section:**
    - Large Status Badge (Yellow/Red/Green).
    - Batch ID (e.g., #Batch-9921).
    - 3-Column Stats: Total Items | Success Count | Error Count (Highlighted).

- **Body Section (The Error Table):**
    - Render this ONLY if there are errors/warnings.
    - **Table Columns:**
        - **Platform:** Icon (Google/Meta).
        - **Issue:** Bold Title (e.g., "Store Code Mismatch") + Gray Code (`invalid_store_code`).
        - **Impact:** Badge (High/Medium).
        - **Count:** Number of items.
        - **Solution:** A short hint text (e.g., "Check GMC Linking").
    - **Interaction:** Clicking a row expands a small "Sample List" showing 3 SKU IDs (e.g., "SKU-101, SKU-102...").

- **Footer Section:**
    - **Primary Button:** "Download Error Report (.CSV)" (Full width, white).
    - **Secondary Button:** "View in Platform" (Link to Google/Meta).

**Mock Data for the Drawer (Use these specific API errors):**
- **Scenario 1:** Google Warning
    - Error: `invalid_store_code` ("Store Code Not Found") - Count: 450 - Impact: High.
    - Error: `price_mismatch` ("Local Price > Online") - Count: 12 - Impact: Low.
- **Scenario 2:** Meta Warning
    - Error: `content_id_mismatch` ("Product Missing in Catalog") - Count: 85.
    - Error: `rate_limit_exceeded` ("Throttled") - Count: 1 (Batch-level).

**Technical:**
- Use 'Lucide React' for icons (Activity, AlertTriangle, CheckCircle, XCircle).
- Ensure the 'Review' button in the Sidebar actually opens the Drawer state.