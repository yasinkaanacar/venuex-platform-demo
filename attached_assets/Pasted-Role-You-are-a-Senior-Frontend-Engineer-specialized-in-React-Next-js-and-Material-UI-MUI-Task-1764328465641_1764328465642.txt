Role: You are a Senior Frontend Engineer specialized in React, Next.js, and Material UI (MUI).

Task: Recreate a pixel-perfect, responsive "Setup/Onboarding" page for a B2B SaaS platform called "VenueX". Do not use the raw HTML I might have pasted earlier; instead, build this from scratch using the specifications below to ensure clean, maintainable code.

Tech Stack:

React (Functional Components, Hooks)

Material UI (MUI) v5/v6 (Components & Icons)

Lucide React (for icons)

Tailwind CSS (for layout utility classes, if needed alongside MUI)

Page Layout & Structure:

Sidebar (Navigation):

Theme: Dark Blue background (#0C3150).

Logo: VenueX Logo at the top.

Menu Items:

Dashboard

Locations Group: Locations, Reviews

Sales Group: Offline Conversions, Catalog

Management Group: Setup (Active State - Blue highlight), Profile, Settings

Chat Bot: VenueX AI (Beta tag)

Main Content Area (The Wizard):

Header:

Breadcrumbs: Dashboard > Setup

Title: "VenueX Setup"

Description: "On this screen, you can easily complete all the setup steps... (use placeholder text if needed)."

Visual Stepper: A horizontal progress bar displaying 4 steps: 1. Brand Infos, 2. Locations, 3. Sales, 4. Catalog.

The Accordion System (The Core Feature): Use MUI Accordion for the following 4 steps.

Accordion 1: Update Brand Information (Status: Completed/Green Check)

Content: Text explaining brand info consistency.

Action: "Update Informations" button (Outlined).

Accordion 2: Business Profile (Locations & Reviews)

Sub-Section 1: Google Business Profile Access.

Buttons: "Account Connected" (Disabled, Gray), "Reviews Activated" (Disabled, Gray), "Disconnect" (Active).

Sub-Section 2: Check Location Data.

Action: "Locations Checked" button (Disabled).

Sub-Section 3: Other Platforms (Grid Layout).

Create a reusable IntegrationCard component.

Cards:

Apple Business Connect (Image: Apple Logo, Button: "Connect")

Meta Pages (Image: Meta Logo, Button: "Connected" - Disabled)

Yandex Business (Image: Yandex Logo, Button: "Coming Soon" - Disabled)

Togg (Image: Togg Logo, Button: "Connected" - Disabled)

Accordion 3: Sales Data

Flow: Two steps before platforms: 1. "Connect Data Source", 2. "Perform Data Mapping".

Sub-Section: Platforms.

Cards: Google Ads (Connected), Meta Conversions (Connected), TikTok Conversions (Connected).

Accordion 4: Catalog Data

Flow: 1. "Connect Data Source", 2. "Perform Data Mapping".

Sub-Section: Google Merchant Center.

Buttons: "Account Connected", "Configure".

Sub-Section: Other Platforms.

Cards: Meta Catalog (Button: "Connect"), Togg (Button: "Connected").

Implementation Requirements:

Create a separate component for the Sidebar (Sidebar.tsx).

Create a reusable SetupStep component (wrapper for Accordion).

Create a reusable IntegrationCard component (accepts logo, title, description, status).

Use Mock Data arrays for the cards so the code is clean and loopable.

Make it look modern, clean, and professional (B2B SaaS style).

Action: Start by setting up the basic layout (Sidebar + Main Area) and then implement the Accordion steps one by one.