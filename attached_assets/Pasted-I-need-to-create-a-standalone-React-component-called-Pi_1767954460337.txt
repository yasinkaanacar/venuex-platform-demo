I need to create a standalone React component called `PipelineStatus.tsx`.
This component represents the "System Health & Data Flow" header seen in the reference image.

**Visual Style Constraints (STRICT):**
- Background: Dark Mode (bg-[#111827] or similar from the screenshot).
- Card Background: Slightly lighter dark (bg-[#1F2937]).
- Text: White/Gray for labels.
- Accents: Use the exact Green (#10B981) for success/ERP and Blue (#3B82F6) for VenueX Engine from the image.
- Shapes: Rounded corners, pill-shaped status indicators.

**Component Logic & Features:**

1.  **Structure (Left to Right Flow):**
    - **Node 1: Client ERP (Source)**
        - Icon: Green Dot (Status Indicator).
        - Text: "Client ERP".
        - Subtext: "Last sync: [Dynamic Time]" (e.g., '2 min ago').
        - *Interaction:* Hovering shows a tooltip: "Connection Strength: Excellent (24ms)".

    - **Connector 1 (Animated):**
        - A horizontal line with a moving gradient or dash to simulate data flowing from ERP to VenueX.

    - **Node 2: VenueX Engine (The Brain)**
        - Icon: The Blue Lightning Icon (from image).
        - Text: "VenueX Engine".
        - Subtext: "Processing [Count] items".
        - *Interaction:* Hovering shows details: "Avg Processing Time: 1.2s".

    - **Connector 2 (Animated):**
        - Flowing line from VenueX to Destinations.

    - **Node 3: Destinations (The Targets)**
        - **Google Merchant:** Show G Logo + Status Dot.
        - **Meta Commerce:** Show Meta Logo + Status Dot.
        - *Critical:* If one platform is down (e.g., Meta), its dot turns Red/Yellow, but Google stays Green.

    - **Right Side Action:**
        - "Activity Log" button (Ghost button style with an icon).

**Technical Requirements:**
- Use 'framer-motion' or standard CSS keyframes for the "Data Flow" animation on the connector lines.
- Use Tailwind CSS for styling.
- Create clear props for the component so I can feed it data later (e.g., `erpStatus`, `googleStatus`, `processedCount`).