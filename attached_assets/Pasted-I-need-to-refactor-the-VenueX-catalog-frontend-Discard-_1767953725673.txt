I need to refactor the VenueX '/catalog' frontend.
Discard previous styling instructions regarding colors.
Focus STRICTLY on layout structure, data density, and state management.
Use a neutral, grayscale 'wireframe' aesthetic (borders, whitespace, bold text only).

**Core Layout Changes:**

1.  **Layout Grid:**
    - Create a 3-column layout.
    - **Left & Center (75% width):** Main Dashboard area.
    - **Right (25% width):** Permanent "Live Activity Log" sidebar (do not hide this behind a button).

2.  **Top Navigation (The Pipeline):**
    - Full-width distinct header.
    - Center the pipeline visualizer: [ERP] -> [VenueX] -> [Ad Platforms].
    - Make connections clear (arrows).

3.  **Main Area Components:**

    * **A. Financial & Health KPIs (Top Row):**
        - Keep the 4 metrics. Make "Ad Budget Saved" physically larger than the rest to emphasize value.

    * **B. The Search Bar (Divider):**
        - Full width input field. Placeholder: "Enter SKU or Store Code to inspect..."

    * **C. The Content View (Dynamic State):**
        - Create a "Dev Toggle" button in the corner to switch between these two views instantly for review:

        - **VIEW 1: Default Dashboard (The Intelligence View)**
            - Split 50/50 horizontally.
            - **Left: The Matrix.** Do NOT use cards. Use a 2x2 Quadrant Grid with borders.
                - Top-Left: "Stars" (List top 3 items).
                - Top-Right: "Critical OOS" (List top 3 items).
                - Bottom-Left: "Dead Stock".
                - Bottom-Right: "Ignore".
            - **Right: The Issues Table.** Compact rows. Columns: Error Type, Count, Platform Icon.

        - **VIEW 2: Search Result (The Store Map)**
            - *This view replaces the Matrix/Issues when an SKU is selected.*
            - **Header:** Product Details (Image, Title, SKU, Global Price).
            - **Grid:** A dense grid of "Store Cards".
                - Each card represents a store.
                - Visual Indicator: Green Dot (Available) vs Red Dot (OOS).
                - Text: Store Name + Store Code.
                - Price: Show Local Price if different from Global.

4.  **Right Sidebar (Activity Log):**
    - A scrolling vertical list.
    - Monospaced font for logs.
    - Example: "14:02: Batch #992 synced to Meta (200 OK)".

**Technical Requirement:**
- Ensure both "Default View" and "Search Result View" are coded and accessible via the toggle.
- Do not apply arbitrary colors (blues/greens). Keep it black/white/gray/border-based.