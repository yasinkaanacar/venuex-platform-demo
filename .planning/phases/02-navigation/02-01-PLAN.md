---
phase: 02-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/lib/analytics.ts
  - client/src/components/layout/sidebar.tsx
autonomous: true
requirements: [NAV-01]

must_haves:
  truths:
    - "All ANALYTICS_EVENTS values are prefixed with demo_"
    - "Every capture wrapper injects source: 'demo' into PostHog properties"
    - "Clicking any sidebar nav item fires demo_sidebar_nav_clicked with to, from, and module"
    - "ALL sidebar links are tracked — including Settings, Team/Profile, VenueX AI, and logo"
  artifacts:
    - path: "client/src/lib/analytics.ts"
      provides: "Namespaced event constants and source-injected capture wrappers"
      contains: "demo_sidebar_nav_clicked"
    - path: "client/src/components/layout/sidebar.tsx"
      provides: "Sidebar navigation click tracking"
      contains: "trackSidebarNav"
  key_links:
    - from: "client/src/components/layout/sidebar.tsx"
      to: "client/src/lib/analytics.ts"
      via: "import { trackSidebarNav }"
      pattern: "trackSidebarNav\\("
---

<objective>
Update analytics.ts to namespace all events with `demo_` prefix and inject `source: 'demo'` into every capture call, then instrument sidebar navigation clicks.

Purpose: D-01/D-02/D-03 require namespacing before any tracking calls are added. NAV-01 requires sidebar click tracking. Both are foundational for all subsequent Phase 2 work.
Output: Updated analytics.ts with demo_ prefixed events + sidebar.tsx with trackSidebarNav on every Link.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-navigation/02-CONTEXT.md
@.planning/phases/02-navigation/02-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

@client/src/lib/analytics.ts
@client/src/components/layout/sidebar.tsx
@client/src/routes/paths.ts

<interfaces>
<!-- From client/src/lib/analytics.ts — current state before modification -->
export const ANALYTICS_EVENTS = {
  SIDEBAR_NAV_CLICKED: 'sidebar_nav_clicked',
  TAB_SWITCHED: 'tab_switched',
  LOCATION_VIEWED: 'location_viewed',
  LOCATION_ACTION: 'location_action',
  FILTER_CHANGED: 'filter_changed',
  AI_CHAT_MESSAGE_SENT: 'ai_chat_message_sent',
  AI_EXAMPLE_QUESTION_CLICKED: 'ai_example_question_clicked',
} as const;

export interface SidebarNavClickedProps {
  to: string;
  from: string;
  module: string;
}

export interface TabSwitchedProps {
  module: string;
  tab_name: string;
}

export const trackSidebarNav = (props: SidebarNavClickedProps) => { ... };
export const trackTabSwitch = (props: TabSwitchedProps) => { ... };

<!-- From client/src/routes/paths.ts -->
export const PATHS = {
  HOME: '/',
  LOCATIONS: '/locations',
  REVIEWS: '/reviews',
  CATALOG: '/catalog',
  OFFLINE_CONVERSIONS: '/offline-conversions',
  SEGMENTS: '/segments',
  VENUEX_AI: '/venuex-ai',
  SETTINGS: '/settings',
  PROFILE: '/profile',
  // ... others
} as const;
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Namespace analytics.ts events with demo_ prefix and inject source property (per D-01, D-02, D-03)</name>
  <files>client/src/lib/analytics.ts</files>
  <read_first>
    - client/src/lib/analytics.ts (current state — all 7 event constants and 7 capture wrappers)
    - .planning/phases/02-navigation/02-CONTEXT.md (D-01, D-02, D-03 locked decisions)
  </read_first>
  <action>
Update `client/src/lib/analytics.ts` with two changes:

**Change 1 — ANALYTICS_EVENTS values (per D-01):**
Replace all 7 event name string values with `demo_` prefix:
- `'sidebar_nav_clicked'` -> `'demo_sidebar_nav_clicked'`
- `'tab_switched'` -> `'demo_tab_switched'`
- `'location_viewed'` -> `'demo_location_viewed'`
- `'location_action'` -> `'demo_location_action'`
- `'filter_changed'` -> `'demo_filter_changed'`
- `'ai_chat_message_sent'` -> `'demo_ai_chat_message_sent'`
- `'ai_example_question_clicked'` -> `'demo_ai_example_question_clicked'`

**Change 2 — Capture wrappers inject `source: 'demo'` (per D-02):**
For every capture wrapper that takes props, change the `posthog.capture()` call to spread props and add source:
```typescript
posthog.capture(ANALYTICS_EVENTS.SIDEBAR_NAV_CLICKED, { ...props, source: 'demo' });
```

For `trackAiExampleQuestionClicked` (no props), pass an object:
```typescript
posthog.capture(ANALYTICS_EVENTS.AI_EXAMPLE_QUESTION_CLICKED, { source: 'demo' });
```

**Do NOT change:** TypeScript interfaces (SidebarNavClickedProps, TabSwitchedProps, etc.) must NOT include `source`. The `source` property is injected internally by the wrapper, never by callers (per Pitfall 3 in RESEARCH.md).

Keep all existing try/catch, console.warn, JSDoc header, and import unchanged.
  </action>
  <verify>
    <automated>cd /Users/kaanacar181/Documents/GitHub/venuex-platform-demo && grep -c "demo_" client/src/lib/analytics.ts | grep -q "^7$" && grep -c "source: 'demo'" client/src/lib/analytics.ts | grep -q "^7$" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - analytics.ts contains exactly 7 occurrences of `demo_` in ANALYTICS_EVENTS values
    - analytics.ts contains exactly 7 occurrences of `source: 'demo'` (one per capture wrapper)
    - analytics.ts does NOT contain `source` in any interface definition (grep for `source.*string` in interface blocks returns 0)
    - `npm run check` does not introduce new TypeScript errors in analytics.ts
  </acceptance_criteria>
  <done>All 7 ANALYTICS_EVENTS values prefixed with demo_, all 7 capture wrappers inject source: 'demo', no interface changes</done>
</task>

<task type="auto">
  <name>Task 2: Instrument sidebar navigation clicks with trackSidebarNav (per D-04, D-05, D-06, NAV-01)</name>
  <files>client/src/components/layout/sidebar.tsx</files>
  <read_first>
    - client/src/components/layout/sidebar.tsx (full file — need to find ALL Link elements across expanded/collapsed/footer render paths)
    - client/src/routes/paths.ts (PATHS constants for MODULE_NAMES map)
    - client/src/lib/analytics.ts (confirm trackSidebarNav export exists)
  </read_first>
  <action>
Modify `client/src/components/layout/sidebar.tsx`:

**Step 1 — Add imports at top of file (after existing imports, following project import order):**
```typescript
import { trackSidebarNav } from '@/lib/analytics';
import { PATHS } from '@/routes/paths';
```
Note: If `PATHS` is already imported, do not duplicate. If `useLocation` from wouter is already imported and destructured as `[location]`, use that existing variable for `from`.

**Step 2 — Add MODULE_NAMES lookup map inside the component or at module level:**
```typescript
const MODULE_NAMES: Record<string, string> = {
  [PATHS.HOME]: 'overview',
  [PATHS.LOCATIONS]: 'locations',
  [PATHS.REVIEWS]: 'reviews',
  [PATHS.CATALOG]: 'catalog',
  [PATHS.OFFLINE_CONVERSIONS]: 'offline-conversions',
  [PATHS.SEGMENTS]: 'segments',
  [PATHS.VENUEX_AI]: 'venuex-ai',
  [PATHS.SETTINGS]: 'settings',
  [PATHS.PROFILE]: 'team',
};
```

**Step 3 — Add onClick to EVERY `<Link>` element in the file (per D-05 — track ALL sidebar items):**

The onClick handler pattern for all Links:
```typescript
onClick={() => trackSidebarNav({ to: href, from: location, module: MODULE_NAMES[href] ?? href })}
```

Where `href` is the link's destination (item.href for navItems loop, or the explicit path for footer/logo links) and `location` is the current route from `useLocation()`.

**Specific Link locations to instrument (per RESEARCH.md Pitfall 1):**
1. **Logo Link** (~line 71-88) — `<Link href={PATHS.HOME}>` — add onClick with `to: PATHS.HOME`
2. **Standard navItems loop** (~line 187-204) — `<Link href={item.href}>` — add onClick with `to: item.href`
3. **VenueX AI expanded Link** (~line 132-183) — `<Link href={item.href}>` — add onClick with `to: item.href`
4. **Collapsed footer Links** (~line 220-241) — Settings and Profile `<Link>` elements
5. **Expanded footer Links** (~line 254-276) — Settings and Profile `<Link>` elements

Do NOT create a wrapper component around Link (per D-04 — inline onClick only).
Do NOT modify any existing styling, conditional rendering, or Link behavior — only ADD onClick attributes.
  </action>
  <verify>
    <automated>cd /Users/kaanacar181/Documents/GitHub/venuex-platform-demo && grep -c "trackSidebarNav" client/src/components/layout/sidebar.tsx | xargs -I{} test {} -ge 5 && grep -q "MODULE_NAMES" client/src/components/layout/sidebar.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - sidebar.tsx contains `import { trackSidebarNav } from '@/lib/analytics'`
    - sidebar.tsx contains `MODULE_NAMES` constant with at least 9 entries (overview, locations, reviews, catalog, offline-conversions, segments, venuex-ai, settings, team)
    - sidebar.tsx contains at least 5 occurrences of `trackSidebarNav(` (logo + navItems loop + VenueX AI + footer settings + footer profile)
    - Every `trackSidebarNav` call passes `{ to: ..., from: location, module: MODULE_NAMES[...] ... }`
    - No new wrapper components created (no `TrackableLink` or similar)
    - `npm run check` does not introduce new TypeScript errors in sidebar.tsx
  </acceptance_criteria>
  <done>Every Link in sidebar.tsx fires trackSidebarNav on click with correct to, from, module properties per D-04/D-05/D-06</done>
</task>

</tasks>

<verification>
1. `grep -c "demo_" client/src/lib/analytics.ts` returns 7
2. `grep -c "source: 'demo'" client/src/lib/analytics.ts` returns 7
3. `grep -c "trackSidebarNav" client/src/components/layout/sidebar.tsx` returns >= 5
4. `grep "MODULE_NAMES" client/src/components/layout/sidebar.tsx` shows the lookup map
5. `npm run check` passes without new errors
</verification>

<success_criteria>
- All ANALYTICS_EVENTS values prefixed with `demo_`
- All capture wrappers inject `source: 'demo'`
- Every sidebar Link element has onClick tracking
- TypeScript compiles without new errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-navigation/02-01-SUMMARY.md`
</output>
